name: Mi primer workflow
on: push

jobs:
  build:
      runs-on: ubuntu-latest
      steps: 
       - uses: actions/checkout@v3 
        
       - name: Run a Sample Github action
         run: echo "Hola ronal"
         
       - name: Probar error
         run: echo "Simular que hay un error"; exit 1
